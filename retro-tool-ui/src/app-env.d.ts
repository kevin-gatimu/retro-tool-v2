/**
 * Build-time constants injected by Vite (see vite.config.ts `define`).
 * `__APP_VERSION__` carries the release-please-managed app version, e.g. "v1.1.0".
 */
declare const __APP_VERSION__: string
