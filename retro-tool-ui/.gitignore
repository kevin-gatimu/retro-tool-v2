# agents
.agents/

node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.env.*
!.env.example
!.env.*.example
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# VitePress (user guide under docs/). Built output goes to dist/docs (ignored
# via `dist` above); these are the generator's local scratch dirs.
docs/.vitepress/cache
docs/.vitepress/dist

# Ignore all plain-text files (local secrets/scratch notes); keep the served robots.txt
*.txt
!public/robots.txt
