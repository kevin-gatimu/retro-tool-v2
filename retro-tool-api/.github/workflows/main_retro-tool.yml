# ╔══════════════════════════════════════════════════════════════╗
# ║  DEPRECATED — This file is NOT read by GitHub Actions.     ║
# ║  GitHub only reads workflows from the REPO ROOT at:        ║
# ║    .github/workflows/deploy-api.yml                        ║
# ║  This file is kept for reference only.                     ║
# ╚══════════════════════════════════════════════════════════════╝
name: retro-tool-api ci-cd (DEPRECATED)

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main
  workflow_dispatch:

env:
  IMAGE_NAME: retro-tool-api
  NODE_VERSION: '22'
  ACR_NAME: ''
  ACR_LOGIN_SERVER: ''
  AZURE_CLIENT_ID: ''
  AZURE_TENANT_ID: ''
  AZURE_SUBSCRIPTION_ID: ''
  AKS_RESOURCE_GROUP: ''
  AKS_CLUSTER_NAME: ''

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Type check
        run: pnpm type-check

      - name: Test
        run: pnpm test -- --runInBand

      - name: Build
        run: pnpm build

  publish-image:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    needs: validate
    permissions:
      id-token: write
      contents: read
    outputs:
      image: ${{ steps.image.outputs.image }}
    steps:
      - uses: actions/checkout@v4

      - name: Stop if Azure placeholders are not configured
        if: env.AZURE_CLIENT_ID == '' || env.ACR_NAME == '' || env.ACR_LOGIN_SERVER == ''
        run: |
          echo "Configure Azure login and ACR values in this workflow before enabling deployment."
          exit 1

      - name: Set image metadata
        id: image
        shell: bash
        run: |
          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            ENV_TAG=production
          else
            ENV_TAG=staging
          fi
          IMAGE="${{ env.ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:${ENV_TAG}-${GITHUB_SHA}"
          echo "image=${IMAGE}" >> "$GITHUB_OUTPUT"

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ env.AZURE_CLIENT_ID }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

      - name: Login to ACR
        run: az acr login --name ${{ env.ACR_NAME }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.image.outputs.image }}

  deploy-staging:
    if: github.event_name == 'push' && github.ref_name == 'develop'
    runs-on: ubuntu-latest
    needs: publish-image
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Stop if AKS placeholders are not configured
        if: env.AKS_RESOURCE_GROUP == '' || env.AKS_CLUSTER_NAME == ''
        run: |
          echo "Configure AKS resource group and cluster values in this workflow before enabling deployment."
          exit 1

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ env.AZURE_CLIENT_ID }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

      - name: Set AKS context
        uses: azure/aks-set-context@v4
        with:
          resource-group: ${{ env.AKS_RESOURCE_GROUP }}
          cluster-name: ${{ env.AKS_CLUSTER_NAME }}

      - name: Deploy image to staging
        run: |
          kubectl -n retro-tool-staging set image deployment/retro-tool-api retro-tool-api=${{ needs.publish-image.outputs.image }}
          kubectl -n retro-tool-staging rollout status deployment/retro-tool-api --timeout=180s

  deploy-production:
    if: github.event_name == 'push' && github.ref_name == 'main'
    runs-on: ubuntu-latest
    needs: publish-image
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Stop if AKS placeholders are not configured
        if: env.AKS_RESOURCE_GROUP == '' || env.AKS_CLUSTER_NAME == ''
        run: |
          echo "Configure AKS resource group and cluster values in this workflow before enabling deployment."
          exit 1

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ env.AZURE_CLIENT_ID }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

      - name: Set AKS context
        uses: azure/aks-set-context@v4
        with:
          resource-group: ${{ env.AKS_RESOURCE_GROUP }}
          cluster-name: ${{ env.AKS_CLUSTER_NAME }}

      - name: Deploy image to production
        run: |
          kubectl -n retro-tool-production set image deployment/retro-tool-api retro-tool-api=${{ needs.publish-image.outputs.image }}
          kubectl -n retro-tool-production rollout status deployment/retro-tool-api --timeout=180s
