# Convex Architecture & Usage

How this monorepo uses Convex: the deployment topology, every key/secret, the
data model, how NestJS writes to it, how the UI reads from it, and how it's
authenticated and deployed.

> **Related docs:** [convex-self-hosting.md](../deployment/convex-self-hosting.md) (running the
> self-hosted backend, env vars, admin key, troubleshooting) ·
> [security/authentication.md](../security/authentication.md) (the JWT-auth hardening) ·
> [infra/provisioning.md](../infra/provisioning.md) (deploy-time env setup).

---

## 1. The role of Convex (read the rule first)

**PostgreSQL is the system of record. Convex is a read-only realtime projection.**

- All durable, authoritative state lives in Postgres, written by the NestJS API.
- After every mutation, NestJS **pushes a snapshot** of the affected entity into
  Convex over the HTTP admin API.
- The UI **subscribes** to those Convex snapshots for realtime updates instead of
  polling the REST API.
- **Never** put business logic in a Convex mutation. Convex functions only
  upsert/read projection rows and maintain aggregate counters.

```
              writes (REST)                 push snapshot (HTTP admin API)
   Browser ──────────────────►  NestJS API ──────────────────────────►  Convex
      ▲                          (Postgres =                              (projection
      │  subscribe (WebSocket)    system of record)                        tables)
      └───────────────────────────────────────────────────────────────────┘
```

This is **not** the official `@convex-dev/better-auth` integration — that runs
Better Auth *inside* Convex and would invert the ownership model. We deliberately
use Convex's generic `customJwt` provider so Convex only *verifies* tokens the
external NestJS API issues. See [security/authentication.md](../security/authentication.md).

---

## 2. Deployment topology per environment

| Environment | Hosting | Deployment | Postgres |
| --- | --- | --- | --- |
| **Local** | Self-hosted in Docker (`ghcr.io/get-convex/convex-backend`) | `convex-local` instance, ports 3210 (API), 3211 (HTTP actions), 6791 (dashboard) | shared local Postgres, DB `convex_local` |
| **Staging** | Self-hosted on Azure App Service (`retrotool-staging-convex`) | Instance `retrotool-convex-staging`, provisioned by `infra/convex-staging.bicep` | Dedicated database (`retrotool_convex_staging`) on the shared staging Flexible Server |
| **Production** | Self-hosted on Azure App Service (`retrotool-prod-convex`) | Instance `retrotool-convex-prod`, provisioned by `infra/convex-production.bicep` | Dedicated database (`retrotool_convex_prod`) on the shared production Flexible Server |
| **Develop** | *(not deployed)* | `develop` has no Azure footprint at all — no resource group, App Service, or Convex deployment of any kind. All `develop`-branch work happens against the same local self-hosted Docker instance described in the **Local** row above. | n/a |

> Staging and production migrated from Convex Cloud to self-hosted App Service
> deployments (see [convex-azure-self-hosting-plan.md](../deployment/convex-azure-self-hosting-plan.md),
> [convex-staging-runbook.md](../deployment/convex-staging-runbook.md), and
> [convex-production-runbook.md](../deployment/convex-production-runbook.md)).
> **No environment uses Convex Cloud today** — `develop` was never a deployed
> environment to begin with, so there was never a Convex Cloud project backing it
> either. Production's self-hosted stack is deployed manually (`workflow_dispatch`
> on `deploy-convex-production.yml`) rather than automatically on every push,
> unlike staging.

The browser connects to whatever `VITE_CONVEX_URL` is built into the UI; the API
syncs to whatever `CONVEX_SYNC_URL` it's configured with.

---

## 3. Keys & secrets — what each one is for

Convex involves **five** distinct consumers of env vars; mixing them up is the
most common source of confusion. The categories:

| Variable | Consumer | Purpose |
| --- | --- | --- |
| `CONVEX_SELF_HOSTED_URL` | Convex **CLI** | Which self-hosted backend `convex dev`/`deploy`/`codegen`/`env` target (local, staging, production). |
| `CONVEX_SELF_HOSTED_ADMIN_KEY` | Convex **CLI** | Auth for the CLI against a self-hosted backend. Generated by `./generate_admin_key.sh` in the container — **not** set in advance. Used for local/staging/production. |
| `CONVEX_DEPLOY_KEY` | Convex **CLI** | Auth for the CLI against **Convex Cloud**. **Unused everywhere in this project** — local, staging, and production all self-host, and `develop` isn't a deployed environment at all (it's local-only). Kept empty in the env files solely because the Convex CLI refuses to run when a Cloud deployment ref is set alongside self-hosted vars — see [convex-self-hosting.md](../deployment/convex-self-hosting.md). |
| `CONVEX_CLOUD_URL` / `CONVEX_DEPLOYMENT` | Convex **CLI** | Cloud connection targeting. **Left empty everywhere** — no environment (including `develop`) targets Convex Cloud. |
| `CONVEX_INSTANCE_NAME` | Convex **container** | Logical instance name; Convex derives its Postgres DB name from it (hyphens → underscores). |
| `CONVEX_INSTANCE_SECRET` | Convex **container** | 64-char hex signing/encryption secret. `openssl rand -hex 32`. |
| `CONVEX_POSTGRES_URL` | Convex **container** | Convex's own Postgres server URL — **no database name** (see self-hosting doc). |
| `CONVEX_DO_NOT_REQUIRE_SSL` | Convex **container** | `1` local, `0` prod. |
| `CONVEX_SYNC_URL` | **NestJS API** (runtime) | Base URL for `POST {url}/api/mutation` projection pushes. |
| `CONVEX_SYNC_ADMIN_KEY` | **NestJS API** (runtime) | Sent as `Authorization: Convex <key>` on those pushes; also used for admin-metrics + `clearTables`. |
| `JWT_ISSUER` | **Convex function runtime** | `auth.config.ts` — must byte-match the token `iss` (= the API's `BETTER_AUTH_URL`). String identity check, never fetched. |
| `JWT_AUDIENCE` | **Convex function runtime** | `auth.config.ts` — must match the token `aud` (= the API's `BETTER_AUTH_JWT_AUDIENCE`, default `convex`). |
| `JWT_JWKS_URL` | **Convex function runtime** | `auth.config.ts` — the API's `/api/auth/jwks`, **fetched over the network** to verify RS256 signatures. |
| `VITE_CONVEX_URL` | **UI build** | The Convex deployment URL the browser's `ConvexReactClient` connects to. |
| `VITE_{RETROS,ESTIMATES,ICEBREAKERS,STANDUPS,NOTIFICATIONS}_REALTIME_BACKEND` | **UI build** | Per-feature realtime toggle, read in [realtime-config.ts](../../retro-tool-ui/src/lib/realtime-config.ts). Hardcoded to `convex` in every env file today — Socket.IO gateways were removed from the API, so the `socket-io` enum value the schema still accepts has no live code path behind it. |
| `CONVEX_BETTER_AUTH_URL` | (legacy) | Present in env files/compose but **unused in code** — leftover from an earlier auth approach. Safe to ignore/remove. |

> **The critical distinction:** `JWT_ISSUER`/`JWT_AUDIENCE`/`JWT_JWKS_URL` are read by
> `process.env` **inside Convex functions**, so they live on the **Convex
> deployment** (set via `convex env set`), **not** in any `.env` file. The `.env.*`
> files only configure the CLI/container/API/UI. See
> [convex-self-hosting.md](../deployment/convex-self-hosting.md#function-runtime-variables-read-by-processenv-inside-convex-functions)
> and [infra/provisioning.md](../infra/provisioning.md) step 7.

Code references: API reads `convex.url`/`convex.adminKey` in
[configuration.ts](../../retro-tool-api/src/config/configuration.ts); the sync
services gate on them. Convex reads `process.env.JWT_*` in
[auth.config.ts](../../convex-backend/convex/auth.config.ts). UI reads
`VITE_CONVEX_URL` in [convex-client.ts](../../retro-tool-ui/src/lib/convex-client.ts).

---

## 4. The data model (`convex-backend/convex/`)

### Files

| File | Purpose |
| --- | --- |
| `schema.ts` | All projection tables + indexes. |
| `convex.config.ts` | Registers the rate-limiter and aggregate components. |
| `server.ts` | Custom function wrappers: `query`, `mutation`, `internalQuery`, `internalMutation`. |
| `auth.config.ts` | `customJwt` provider (RS256) — verifies API-issued JWTs. |
| `lib/authz.ts` | `requireIdentity(ctx)` helper. |
| `rateLimits.ts` | Per-entity rate limits for board/notification upserts. |
| `liveRetros.ts` | Retro session/board projections + typing/ready presence. |
| `liveEstimates.ts` | Estimate session/board projections. |
| `liveIcebreakers.ts` | Icebreaker session/board projections + ephemeral `liveIcebreakerReactions` (sendReaction / getRecentReactions). |
| `liveNotifications.ts` | Notification projections. |
| `liveReports.ts` | Aggregate (stat) upserts + read queries. |
| `admin.ts` | `clearTables` (internal, admin-key only). |

### Projection tables (`schema.ts`)

| Table | Key fields | Indexes | Written by | Read by |
| --- | --- | --- | --- | --- |
| `liveRetroSessions` | `retroId`, `teamId`, `status`, discussion pointers | `by_retro_id`, `by_team_id` | `upsertRetroProjection` | retro list sync |
| `liveRetroBoards` | `retroId`, `userId`, `snapshot` (JSON RetroDetail) | `by_retro_id`, `by_retro_user` | `upsertRetroBoard` (per team member) | retro detail sync |
| `liveEstimateSessions` | `sessionId`, `teamId`, `status`, `currentRoundId` | `by_session_id`, `by_team_id` | `upsertSessionProjection` | estimate list sync |
| `liveEstimateBoards` | `sessionId`, `userId`, `snapshot` (JSON session) | `by_session_id`, `by_session_user` | `upsertEstimateBoard` (per member) | estimate detail sync |
| `liveIcebreakerSessions` / `liveIcebreakerBoards` | `sessionId`, `teamId`/`userId`, `snapshot` | `by_session_id`, `by_team_id` / `by_session_user` | `upsertIcebreaker*` (per member) | icebreaker list/detail sync |
| `liveIcebreakerReactions` | `sessionId`, `userId`, `kind` (`celebrate`/`applause`/`fire`), `createdAt` | `by_session_id` | `sendReaction` (client public mutation) | icebreaker presenting phase — confetti burst on every participant |
| `liveStandupEntries` / `liveStandupBoards` | `standupId`, `entryDate`, `userId`, `snapshot` | `by_standup_id`, `by_standup_date` / `by_standup_date_user` | `upsertStandup*` (per member) | standup list/detail sync |
| `livePolls` | `pollId`, `teamId`, `isClosed` (lightweight signal) | `by_poll_id`, `by_team_id` | `upsertPollProjection`/`deletePollProjection` | UI invalidate + REST refetch |
| `liveSurveys` | `surveyId`, `teamId`/`organizationId`, `isClosed` (lightweight signal) | `by_survey_id`, `by_team_id`, `by_org_id` | `upsertSurveyProjection`/`deleteSurveyProjection` | UI invalidate + REST refetch |
| `liveNotifications` | `notificationId`, `userId`, `read`, body fields | `by_notification_id`, `by_user_id`, `by_user_read` | `upsert*`/`mark*ReadProjection` | notification bell/list |
| `liveTeamMembers` | `teamId`, `userId`, `role` (security projection) | `by_user_id`, `by_team_user` | `upsertMembership`/`pruneStaleMemberships` | authz for every projection query |
| `liveTyping` | `retroId`, `userId`, `displayName` | `by_retro`, `by_retro_user` | `startTyping`/`stopTyping` (client) | retro detail |
| `liveReadyStatus` | `retroId`, `userId`, `isReady` | `by_retro`, `by_retro_user` | `setReadyStatus`/`clearAllReady` (client) | retro detail |
| `livePresence` | `scopeType`, `scopeId`, `userId`, heartbeat | `by_scope`, `by_scope_user` | (schema only — no functions yet) | — |

Boards are stored **per user** (a JSON snapshot keyed by `retroId`/`sessionId` +
`userId`), so a user can only fetch their own snapshot — authorization is just
"this row's `userId` == my identity."

### Components (`convex.config.ts`)

- **`@convex-dev/rate-limiter`** — one instance; named limits in `rateLimits.ts`:
  `retroBoardUpsert` (10/s, burst 20), `estimateBoardUpsert` (10/s, burst 20),
  `notificationUpsert` (50/min).
- **`@convex-dev/aggregate`** — seven denormalized counters maintained by the
  `upsert*Stat` functions in `liveReports.ts`, used for the reports/analytics
  pages:

  | Aggregate | Key tuple | Tracks |
  | --- | --- | --- |
  | `retrosCompletedByTeam` | `[teamId, completedAtMs]` | retro completions per team |
  | `cardsByTeamUser` | `[teamId, userId, createdAtMs]` | cards per team+user |
  | `votesByTeamUser` | `[teamId, userId, createdAtMs]` | votes per team+user |
  | `actionItemsByTeamStatus` | `[teamId, status, createdAtMs]` | action items by status |
  | `estimateSessionsByTeam` | `[teamId, completedAtMs]` | estimate sessions per team |
  | `systemRetros` | `[createdAtMs]` | global retro count |
  | `systemUsers` | `[createdAtMs]` | global user count |

---

## 5. How NestJS writes to Convex (the sync layer)

Every push uses the same HTTP admin call (no typed client):

```
POST {CONVEX_SYNC_URL}/api/mutation
Authorization: Convex {CONVEX_SYNC_ADMIN_KEY}
Content-Type: application/json

{ "path": "liveRetros:upsertRetroProjection", "args": { … }, "format": "json" }
```

Each domain's sync service owns a **private** `runMutation(path, args)` helper (no shared runner) that
no-ops when Convex isn't configured. `ConvexAdminService.runMutation` in
[convex-admin.service.ts](../../retro-tool-api/src/convex-admin/convex-admin.service.ts) is a
separate, admin-only mutation runner used only for `clearTables` — the sync services below do not
call into it:

| Service | Triggers on | Calls |
| --- | --- | --- |
| [retros-projection-sync.service.ts](../../retro-tool-api/src/retros/retros-projection-sync.service.ts) | retro REST mutations (via the projection outbox) | `liveRetros:upsertRetroProjection`, then `upsertRetroBoard` for every team member; `deleteRetroProjection` |
| [estimates-projection-sync.service.ts](../../retro-tool-api/src/estimates/estimates-projection-sync.service.ts) | estimate REST mutations | `liveEstimates:upsertSessionProjection` + `upsertEstimateBoard` per member; `deleteSessionProjection` |
| [notifications-projection-sync.service.ts](../../retro-tool-api/src/notifications/notifications-projection-sync.service.ts) | notification create / read | `liveNotifications:upsert…`, `markNotificationReadProjection`, `markAllNotificationsReadProjection` |
| [teams-members-projection-sync.service.ts](../../retro-tool-api/src/teams/teams-members-projection-sync.service.ts) | team membership add/remove (via the projection outbox) | `liveTeamMembers:upsertMembership`, `deleteMembership`, `pruneStaleMemberships` (also has a `runMutationForResult` variant for the batched prune loop) |
| [icebreakers-projection-sync.service.ts](../../retro-tool-api/src/icebreakers/icebreakers-projection-sync.service.ts) | icebreaker REST mutations (via the projection outbox) | `liveIcebreakers:upsertSessionProjection`, then `upsertIcebreakerBoard` for every team member; `deleteSessionProjection` |
| [standups-projection-sync.service.ts](../../retro-tool-api/src/standups/standups-projection-sync.service.ts) | standup entry REST mutations (via the projection outbox; entity key is the composite `standupId:date`) | `liveStandups:upsertEntryProjection`, then `upsertStandupBoard` for every team member; `deleteStandupProjection` |
| [polls-projection-sync.service.ts](../../retro-tool-api/src/polls/polls-projection-sync.service.ts) | poll REST mutations (via the projection outbox) | `livePolls:upsertPollProjection`; `deletePollProjection` |
| [surveys-projection-sync.service.ts](../../retro-tool-api/src/surveys/surveys-projection-sync.service.ts) | survey REST mutations (via the projection outbox) | `liveSurveys:upsertSurveyProjection`; `deleteSurveyProjection` |

Board snapshots are computed **per team member** because each user's board view
can differ (carried-forward items, permissions baked into the snapshot).

**Aggregates** are written by the API calling the `upsert*Stat` functions on
entity lifecycle events. **`clearTables`** (admin-only) is invoked by the
super-admin endpoint and a scheduled cron
([convex-admin-cron.service.ts](../../retro-tool-api/src/convex-admin/convex-admin-cron.service.ts))
to purge stale projection rows, surfaced in the admin UI at `/admin/convex`
(immediate "Clear Tables Now" + a recurring "Scheduled Cleanup").

`clearTables` enforces an allowlist — the `CLEARABLE_TABLES` constant in
[admin.ts](../../convex-backend/convex/admin.ts) — and the UI's table list in
[convex.tsx](../../retro-tool-ui/src/routes/admin/convex.tsx) mirrors it exactly.
Clearable set: `liveRetroSessions`, `liveEstimateSessions`,
`liveIcebreakerSessions`, `liveRetroBoards`, `liveEstimateBoards`,
`liveIcebreakerBoards`, `liveStandupEntries`, `liveStandupBoards`,
`liveNotifications`, `livePolls`, `liveSurveys`, `livePresence`, `liveTyping`,
`liveReadyStatus`. Each is either reconcilable from PostgreSQL or ephemeral, so
clearing it is recoverable.

> **`liveIcebreakerReactions` is intentionally NOT clearable** — it is a
> high-frequency transient signal that self-expires via a 15-second TTL
> (`REACTION_TTL_MS`). Rows are pruned opportunistically by `sendReaction` on
> each write; no admin intervention is needed.
>
> **`liveTeamMembers` is deliberately NOT clearable.** It is the membership
> projection every projection query authorizes against; a one-click or weekly
> wipe would drop all Convex authz until a reconcile/self-heal runs. It is kept
> fresh by `syncAllMemberships` reconciliation plus a nightly self-heal instead.
> The scheduled cron's `tablesToClear` defaults to empty and only ever contains
> what an admin explicitly saves, so adding a table to the allowlist never
> auto-schedules it.

---

> **Why admin key can call internal functions:** the `/api/mutation` endpoint with
> `Authorization: Convex <key>` is exactly how Convex's own admin client invokes
> functions, and admin auth bypasses the "internal = not callable from clients"
> restriction. That's what lets the server-driven `internalMutation`s (below) stay
> callable by NestJS while being unreachable from the browser.

---

## 6. How the UI reads from Convex

- **Client:** [convex-client.ts](../../retro-tool-ui/src/lib/convex-client.ts) lazily
  creates one `ConvexReactClient(VITE_CONVEX_URL)`; returns `null` if unset.
- **Provider + auth:** [realtime-providers.tsx](../../retro-tool-ui/src/lib/realtime-providers.tsx)
  wraps the app in `ConvexProviderWithAuth` with a `useAuth` hook that fetches the
  JWT from `GET /api/auth/token` (see §7). Falls back to plain children when
  Convex is unconfigured.
- **Feature toggle:** [realtime-config.ts](../../retro-tool-ui/src/lib/realtime-config.ts)
  reads `VITE_*_REALTIME_BACKEND`. `usesConvexForRetros()` etc. gate each feature on the flag
  being `convex` **and** a `VITE_CONVEX_URL` being configured; every env file hardcodes the flags
  to `convex` today. There is no live Socket.IO alternative to roll back to — the type still
  accepts a vestigial `'socket-io'` value (`packages/shared/contracts/src/realtime.ts`), but no
  code path implements it since the gateways were removed. The only actual fallback is REST
  polling (`refetchInterval`), which resumes automatically when Convex is unconfigured.
- **Sync components** subscribe to a board query and hydrate the TanStack Query
  cache from the snapshot (replacing REST polling). They gate on
  `useConvexAuth().isAuthenticated` and pass **no `userId`** (derived server-side):
  - [retro-convex-sync.tsx](../../retro-tool-ui/src/routes/retros/components/retro-convex-sync.tsx) → `liveRetros:getRetroBoard`
  - [estimate-convex-sync.tsx](../../retro-tool-ui/src/routes/estimate/components/estimate-convex-sync.tsx) → `liveEstimates:getEstimateBoard`
  - [notification-convex-sync.tsx](../../retro-tool-ui/src/components/notification-convex-sync.tsx) → `liveNotifications:listUserNotifications`
  - list syncs: `retro-list-convex-sync.tsx`, `estimate-list-convex-sync.tsx`
- **Direct presence** in [$retroId.tsx](../../retro-tool-ui/src/routes/retros/$retroId.tsx)
  uses `useConvexMutation`/`useConvexQuery` for typing + ready status.

Function references are string-cast (`'liveRetros:getRetroBoard' as unknown as
FunctionReference<'query'>`) because the UI doesn't import Convex's generated API.

---

## 7. Authentication (current)

Convex verifies the RS256 JWTs the NestJS API issues; it does not manage sessions.

1. **API issues** a JWT at `GET /api/auth/token` (Better Auth `jwt` plugin, keys
   served at `GET /api/auth/jwks`). Claims: `iss`=API origin, `aud`=`convex`,
   `sub`=Better Auth user id, `role`, 15-min expiry.
2. **UI attaches** it via `ConvexProviderWithAuth` (§6).
3. **Convex verifies** via the `customJwt` provider in
   [auth.config.ts](../../convex-backend/convex/auth.config.ts): matches `iss`/`aud`,
   fetches `JWT_JWKS_URL` to check the RS256 signature.
4. **Functions authorize** with `requireIdentity(ctx)`
   ([lib/authz.ts](../../convex-backend/convex/lib/authz.ts)) and compare
   `identity.subject` (= the user id stored in projection rows). It deliberately
   uses `subject`, **not** `tokenIdentifier` (which is `sub`+`iss` and wouldn't
   match).

### Public vs internal functions

- **`internalMutation`/`internalQuery`** — server-driven, callable only via the
  admin-key `/api/mutation` path (not the browser): all `upsert*`/`delete*`
  projections, the `upsert*Stat` aggregates, and `admin:clearTables`.
- **Public `query`/`mutation`** — browser-callable but every one calls
  `requireIdentity`: board reads derive the user from the JWT; typing/ready
  mutations write under `identity.subject`.

> **Residual:** team-scoped reads enforce *authenticated* but not *team
> membership* (Convex holds no membership data). JWT TTL means Convex authz lags
> session revocation by ≤15 min. Both are acceptable for read-only projections and
> tracked in [security/authentication.md](../security/authentication.md).

---

## 8. Deploy & codegen workflow

Scripts in [convex-backend/package.json](../../convex-backend/package.json):

| Command | Targets | Notes |
| --- | --- | --- |
| `pnpm --filter convex-backend dev` | local self-hosted (`.env.local`) | watch + push functions |
| `pnpm --filter convex-backend dev:staging` | staging, per `.env.staging-local` | |
| `pnpm --filter convex-backend dev:prod` | production, per `.env.production-local` | |
| `pnpm --filter convex-backend codegen` | current deployment | regenerates `_generated/` types |
| `pnpm --filter convex-backend deploy` | target per env file | pushes functions |

> `convex-backend/.env.production.example` already documents the self-hosted setup
> (`CONVEX_SELF_HOSTED_URL`/`CONVEX_SELF_HOSTED_ADMIN_KEY` targeting
> `infra/convex-production.bicep`, like staging) — its `CONVEX_DEPLOY_KEY` /
> Convex Cloud block is present but explicitly marked inactive. Verify against
> your real `.env.production-local` rather than trusting the example file for
> actual secret values.

**Staging and production function deploys are automated.**
[`deploy-convex.yml`](../../.github/workflows/deploy-convex.yml) (staging, on
every push to `staging`) and
[`deploy-convex-production.yml`](../../.github/workflows/deploy-convex-production.yml)
(production, `workflow_dispatch` only) both provision/update the self-hosted
Web App via Bicep and then run `pnpm exec convex deploy` against it. The
`dev`/`dev:staging`/`dev:prod` scripts above are for local iteration against
whichever deployment the loaded `.env*` file targets — they are not what ships
to staging/production. The pipelines additionally wire the *consumers*:
`deploy-api.yml` sets `CONVEX_SYNC_URL`/`CONVEX_SYNC_ADMIN_KEY` on the API;
`deploy-ui.yml` validates and bakes `VITE_CONVEX_URL` into the UI build.

**Per-deployment one-time setup** (after first deploy, and whenever the API host
changes): set `JWT_ISSUER`, `JWT_AUDIENCE`, `JWT_JWKS_URL` on the Convex
deployment — [infra/provisioning.md](../infra/provisioning.md) step 7. The two
deploy workflows above do this automatically on every run for staging/production.

---

## 9. Operational gotchas

- **Self-hosted admin key is ephemeral** — regenerated whenever the container is
  recreated; update it in both env files. See
  [convex-self-hosting.md](../deployment/convex-self-hosting.md#admin-key-is-tied-to-the-running-container).
- **`localhost` inside the Convex container is the container**, so `JWT_JWKS_URL`
  for local must be `http://host.docker.internal:8000/api/auth/jwks` (or the
  compose service name), not `localhost:8000`.
- **`JWT_ISSUER` byte-mismatch** (scheme/port/trailing slash) silently breaks all
  Convex auth — every query throws `Unauthenticated`. Decode a token and compare
  `iss`.
- **Convex env var changes are evaluated at deploy time** — re-deploy functions
  after changing `JWT_*` if they don't take effect.
